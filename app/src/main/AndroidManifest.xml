<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dance_world">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ToolbarActivity"></activity>
        <activity android:name=".WorkshopActivity" />
        <activity android:name=".DjActivity" />
        <activity android:name=".ArtistsActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".NavigationActivity" />
        <activity android:name=".MapsActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".MainActivity"/>
        <activity android:name=".MasterViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCdqs9Bo2ZXij3n1pfDtJIR8aHaxW8iaVo" />
    </application>

</manifest>